<?php
    $wytwornia = $_POST['wytwornia'];

    $con =  createConnection();
    
    $query = $con -> query("INSERT into `wytwornia` values(null, ".w($wytwornia).")");

    $con -> close();

    header('Location: '.config('site_url').'/panel');
    exit();