<?php
        $gatunek = $_POST['gatunek'];

        $con =  createConnection();
        
        $query = $con -> query("INSERT into `gatunki` values(null, ".w($gatunek).")");

        $con -> close();

        header('Location: '.config('site_url').'/panel');
        exit();