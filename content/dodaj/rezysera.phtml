<?php
    $imieR = $_POST['imieR'];
    $nazwiskoR = $_POST['nazwiskoR'];

    $con =  createConnection();
    
    $query = $con -> query("INSERT into `rezyser` values(null, ".w($imieR).", ".w($nazwiskoR).")");

    $con -> close();

    header('Location: '.config('site_url').'/panel');
    exit();